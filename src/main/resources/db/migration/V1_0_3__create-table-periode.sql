-- Table: periode

-- DROP TABLE periode;

CREATE TABLE IF NOT EXISTS periode
(
    periode_id integer NOT NULL GENERATED BY DEFAULT AS IDENTITY ( INCREMENT 1 START 1 MINVALUE 1 MAXVALUE 2147483647 CACHE 1 ),
    fom_dato date NOT NULL,
    til_dato date,
    stønadsendring_id integer NOT NULL,
    beløp float,
    valutakode varchar(10),
    resultatkode varchar(255) NOT NULL,
    referanse varchar(32),
    CONSTRAINT periode_pkey PRIMARY KEY (periode_id),
    CONSTRAINT fk_stonad_id FOREIGN KEY (stønadsendring_id)
        REFERENCES stønadsendring (stønadsendring_id) MATCH SIMPLE
        ON UPDATE NO ACTION
        ON DELETE NO ACTION,
    UNIQUE (stønadsendring_id, fom_dato)
)
    TABLESPACE pg_default;

CREATE INDEX idx_periode_1 ON periode(stønadsendring_id);